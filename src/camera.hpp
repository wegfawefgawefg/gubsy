// Camera follow utilities.
// Responsibility: position the play camera each tick based on player and mouse.
#pragma once

// Smoothly follow player and mouse mix into gg->play_cam.pos.
void update_camera_follow();
