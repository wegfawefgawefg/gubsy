// Projectile stepping and hit resolution.
// Responsibility: advance projectiles, apply hits (damage, metrics, drops).
#pragma once

// Step projectiles and resolve hits (damage, drops, metrics).
void step_projectiles_and_hits();
